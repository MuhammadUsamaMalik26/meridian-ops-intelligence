# Raw CSVs are regenerable but large; not committed
data/raw/*.csv
data/*.duckdb.wal

# Pre-built warehouse and model ARE committed (see README) so the
# deployed Streamlit app doesn't need to run dbt/generate data at runtime.
# data/meridian.duckdb   <- intentionally NOT ignored
# ml/output/             <- intentionally NOT ignored

# dbt
meridian_dbt/target/
meridian_dbt/logs/
meridian_dbt/dbt_packages/
meridian_dbt/.user.yml

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# OS / editor
.DS_Store
.vscode/
